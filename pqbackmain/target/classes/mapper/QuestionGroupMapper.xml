<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.QuestionGroupMapper">
    <select id="getQidByFileId" parameterType="long" resultType="long">
        SELECT qid FROM question_group WHERE file_id = #{fileId}
    </select>
</mapper> 