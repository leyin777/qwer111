<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.DiscussionMapper">
    <resultMap id="DiscussionResultMap" type="com.example.entity.Discussion">
        <id column="id" property="id" />
        <result column="qid" property="qid" />
        <result column="uid" property="uid" />
        <result column="content" property="content" />
        <result column="created_at" property="createdAt" />
    </resultMap>
    <select id="getDiscussionsByQid" resultMap="DiscussionResultMap">
        SELECT id, qid, uid, content, created_at
        FROM discussions
        WHERE qid = #{qid}
        ORDER BY created_at DESC
    </select>
    <insert id="insertDiscussion" parameterType="com.example.entity.Discussion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO discussions (qid, uid, content, created_at)
        VALUES (#{qid}, #{uid}, #{content}, NOW())
    </insert>
    <delete id="deleteDiscussionById">
        DELETE FROM discussions WHERE id = #{id}
    </delete>
    <delete id="deleteDiscussionsByQid">
        DELETE FROM discussions WHERE qid = #{qid}
    </delete>
</mapper> 